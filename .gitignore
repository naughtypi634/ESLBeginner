# Generated build artifacts
PDF/*
output/
*.pdf
*.html
# Only the travel handbook PDF is tracked and pushed to GitHub
!PDF/旅游英语实用手册.pdf

# LaTeX build intermediates & previews (keep build/build_pdfs.py, preamble.tex, template.tex tracked)
build/tex/
build/preview/
build/out/

# Python (if ever added) — keep the venv project-local, never shared
.venv/
__pycache__/
*.pyc
.pytest_cache/

# OS / editor junk
Thumbs.db
.DS_Store
