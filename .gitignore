# Generated build artifacts
output/

# Cached webfonts (not a document)
MD/_fonts_cache/

# LaTeX build intermediates & previews (keep build/build_pdfs.py, preamble.tex, template.tex tracked)
build/tex/
build/preview/
build/out/
build/backups/

# Python (if ever added) — keep the venv project-local, never shared
.venv/
__pycache__/
*.pyc
.pytest_cache/

# OS / editor junk
Thumbs.db
.DS_Store
